import { createContext } from 'react'

const Context = createContext({})

export const ExchangeListingsContextProvider = Context.Provider
export const ExchangeListingContextConsumer = Context.Consumer
