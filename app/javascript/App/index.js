/*
 * This app is not an SPA, and just looks for 
 * events/elements in order to load libraries or
 * render some simple React components.
 * 
 * The "modules" dir contains non-React code, though
 * does import React components.
 */
import './modules/coins'
import './modules/navigation'
