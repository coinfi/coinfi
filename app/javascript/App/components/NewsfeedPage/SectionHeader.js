import React from 'react'

const SectionHeader = ({ children }) => (
  <div className="pa3 bb b--athens-dark flex justify-between items-center bg-athens">
    {children}
  </div>
)

export default SectionHeader
