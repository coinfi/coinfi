import React from 'react'

const SectionHeader = ({ children }) => (
  <div className="pa3 b--b b--athens-dark flex-none flex justify-between items-center">
    {children}
  </div>
)

export default SectionHeader
